# 博雅课线上只读核验（2026-08-26）

本记录基于用户已完成登录与人机认证后的选课页面，只做页面导航、校区切换和查询筛选，没有提交任何选课请求，也没有保存账号、密码、Cookie 或 token。

## 1. 线上环境

- 当前轮次：`2026-2027-1学期本科生正选`
- 当前轮次状态：页面显示正在进行；起止时间为 `2026-08-24 10:00` 至 `2026-08-28 23:55`
- 用户年级口径：页面显示为 2024 级；页面提示 2024 级及以后使用“博雅课类别(2024)”
- 查询页面：登录后的“博雅教育课程”页

## 2. 实际使用的筛选

本次验证使用页面表单的服务端筛选，不以首页展示的前 10 行作为完整结果判断：

| 页面条件 | 实际值 | 含义 |
|---|---:|---|
| 是否冲突 `#public_sfct` | `0` | 不冲突 |
| 是否已满 `#public_sfym` | `0` | 未满 |
| 博雅课类别 `#public_xgxklb` | 空 | 不限定类别 |
| 课程板块 `#course_section` | 空 | 不限定板块 |
| 关键字 `#publicSearch` | 空 | 不限定关键词 |

对应源码请求为 `POST /sys/xsxkapp/elective/publicCourse.do`，`querySetting` 中的 `campus` 随校区切换，`teachingClassType` 为 `XGXK`。冲突判断由系统结合当前登录学生的已有课程完成。

## 3. 五个校区的只读结果

“无筛选首页行数/总页数”只用于说明列表规模；最终结论以服务端筛选结果的行数为准。

| 校区 | 无筛选首页行数/总页数 | 仅筛“未满” | 仅筛“不冲突” | 同时筛“未满+不冲突” |
|---|---:|---:|---:|---:|
| 随园校区 | 10 / 2 | 1 | 2 | **0** |
| 仙林校区 | 10 / 7 | 0 | 7 | **0** |
| 仙林老北 | 0 / 0 | 0 | 0 | **0** |
| 仙林新北 | 2 / 1 | 0 | 0 | **0** |
| 仙林湖公寓 | 0 / 0 | 0 | 0 | **0** |

最终还在随园校区重新执行了一次 `不冲突 + 未满` 组合查询，页面返回 0 行，复核结果一致。

## 4. 对照结果与原因

### 随园校区

唯一的“未满”课程是：

- `文化才艺实践与展示`，教学班 `1000000552[01]`
- 已选约 `37/40`
- 页面标记为冲突，因此不能作为“无冲突、未满”的候选

“不冲突”筛出的两门课程均已满：

- `中国地域文化概论`，教学班 `1000000023[01]`，容量 50
- `流行音乐概论`，教学班 `1000000117[02]`，容量 30

### 仙林校区

“不冲突”筛出 7 个教学班，但均已满；“未满”筛选返回 0 行。因此当前没有符合两个条件交集的课程。

### 仙林新北及其他校区

仙林新北当前展示的两门课程均为已满且冲突；仙林老北、仙林湖公寓没有返回博雅课列表。

## 5. 与源码结论的对应

源码中的 `checkConflict=0` 和 `checkCapacity=0` 正好对应页面的“无冲突”和“未满”。列表中的 `isConflict`、`conflictDesc`、`isFull`、`numberOfFirstVolunteer` 与 `classCapacity` 用于展示和二次校验。本次线上结果说明：服务端确实执行了这两个筛选条件，当前五个校区没有返回同时满足条件的教学班。

公共课点击链路最终会生成 `addParam` 并调用 `volunteer.do`，但本次没有触发该请求；没有产生选课、志愿或候补变更。

## 6. 当前结论与安全下一步

截至本次查询，没有可以直接提交的“博雅课：不冲突、未满”候选。下一次操作应在课程名额发生变化后重新执行同一组校区与筛选查询；如果要考虑“有名额但冲突”或“无冲突但已满”的课程，需要先由用户明确指定例外规则和目标教学班，不能自动替代判断或提交。
